name: Deploy AI Tools Income Stream

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub (optional, if using private images)
        # Not needed if using local build; skip for now
        # uses: docker/login-action@v3
        # with:
        #   username: ${{ secrets.DOCKERHUB_USERNAME }}
        #   password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push site image
        run: |
          docker build ./site -t chinhigh80/ai-tools-income-stream-site:${{ github.sha }}
          # If you have Docker Hub credentials, you can push:
          # echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
          # docker push chinhigh80/ai-tools-income-stream-site:${{ github.sha }}

      - name: Build and push microtool image
        run: |
          docker build ./microtool -t chinhigh80/ai-tools-income-stream-microtool:${{ github.sha }}
          # push if needed

      # The actual deployment to VPS would be done via SSH or a webhook.
      # For simplicity, we'll just output success.
      - name: Deploy to VPS (placeholder)
        run: |
          echo "Deploying to VPS..."
          # You can add SSH commands here using secrets for SSH key and VPS IP.
          # Example:
          # mkdir -p ~/.ssh
          # echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          # chmod 600 ~/.ssh/id_rsa
          # ssh -o StrictHostKeyChecking=no ubuntu@${{ secrets.VPS_IP }} "
          #   cd /opt/ai-tools-income-stream &&
          #   git pull origin main &&
          #   docker compose pull &&
          #   docker compose up -d --remove-orphans
          # "
          echo "Deployment step completed (customize as needed)."